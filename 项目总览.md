# 团队文件管理系统 - 完整项目文档

## 项目简介

一个基于 FastAPI + Vue 3 的团队文件管理系统，支持标签化文件共享，适用于 300 人规模的团队。

## 技术架构

### 后端
- **框架**: FastAPI
- **ORM**: SQLAlchemy
- **数据库**: SQLite (开发环境)
- **端口**: 8080

### 前端
- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **UI 组件库**: Element Plus
- **端口**: 5173

## 项目结构

```
TeamHub/
├── backend/                    # 后端代码（根目录）
│   ├── main.py                # FastAPI 应用主入口
│   ├── models.py              # 数据库模型
│   ├── database.py            # 数据库配置
│   ├── requirements.txt       # Python 依赖
│   └── storage/               # 文件存储目录
│
└── frontend/                   # 前端代码
    ├── src/
    │   ├── App.vue            # 根组件
    │   ├── main.js            # 入口文件
    │   ├── api/               # API 请求封装
    │   ├── components/        # Vue 组件
    │   └── utils/              # 工具函数
    ├── package.json           # Node.js 依赖
    ├── vite.config.js         # Vite 配置
    └── index.html             # HTML 模板
```

## 快速开始

### 1. 启动后端服务

```bash
# 安装 Python 依赖
pip install -r requirements.txt

# 启动后端服务
python main.py
```

后端将在 `http://localhost:8080` 启动。

### 2. 启动前端服务

```bash
# 进入前端目录
cd frontend

# 安装 Node.js 依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

前端将在 `http://localhost:5173` 启动。

### 3. 访问应用

打开浏览器访问：**http://localhost:5173**

## 核心功能

### 后端 API

1. **POST /upload** - 上传文件（支持标签）
2. **GET /tags** - 获取所有标签
3. **POST /search** - 搜索文件（支持关键词和标签）

### 前端功能

1. **文件上传**
   - 拖拽上传
   - 上传前添加标签
   - 文件查重（SHA-256）
   - 重复文件提示

2. **文件搜索**
   - 关键词搜索
   - 标签筛选（渐进式）
   - 组合搜索

3. **文件列表**
   - 文件信息展示
   - 标签彩色显示
   - 文件大小格式化
   - 上传时间显示

## 开发说明

### 后端开发

- 数据库文件：`teamhub.db` (SQLite)
- 文件存储：`storage/` 目录
- API 文档：http://localhost:8080/docs

### 前端开发

- 开发模式：`npm run dev`
- 构建生产：`npm run build`
- API 代理：通过 Vite 代理到后端

### CORS 配置

后端已配置 CORS，允许以下来源：
- http://localhost:5173 (Vite 默认端口)
- http://localhost:3000
- http://127.0.0.1:5173

## 注意事项

1. **端口冲突**：如果 8080 或 5173 端口被占用，请修改相应配置
2. **文件查重**：基于 SHA-256 哈希值，相同文件无法重复上传
3. **标签规范**：标签会自动转为小写并去重
4. **文件存储**：文件存储在 `storage/` 目录，文件名格式为 `{hash前8位}_{原始文件名}`

## 后续扩展建议

- [ ] 用户认证和权限管理
- [ ] 文件下载接口
- [ ] 文件删除接口
- [ ] 文件预览功能
- [ ] 文件版本管理
- [ ] 文件访问日志
- [ ] 文件分享链接
- [ ] 批量操作功能

## 许可证

MIT License
