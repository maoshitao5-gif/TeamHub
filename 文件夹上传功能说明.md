# 文件夹上传功能说明

## 功能概述

已成功实现文件夹上传功能，支持将整个项目文件夹上传并保持文件夹结构完整性。

## 实现的功能

### 1. 数据库模型更新
- 在 `File` 表中添加了 `relative_path` 字段，用于存储文件在文件夹中的相对路径
- 该字段为可选字段（nullable=True），兼容单文件上传

### 2. 后端接口
- **新增接口**: `POST /upload-folder`
  - 支持批量上传多个文件
  - 接收文件列表和对应的相对路径列表
  - 保持文件夹结构存储在 `storage/` 目录下
  - 返回上传成功和失败的文件列表

### 3. 前端功能
- **上传模式选择**: 单文件上传 / 文件夹上传
- **文件夹选择**: 使用 HTML5 的 `webkitdirectory` 属性支持文件夹选择
- **文件夹预览**: 显示文件夹中的所有文件及其相对路径
- **批量上传**: 一次性上传文件夹中的所有文件，保持文件夹结构

## 使用方法

### 前端操作步骤

1. **选择上传模式**
   - 在上传页面，选择"文件夹上传"模式

2. **选择文件夹**
   - 点击上传区域或拖拽文件夹到上传区域
   - 浏览器会打开文件夹选择对话框

3. **预览文件夹内容**
   - 选择文件夹后，会显示文件夹中的所有文件列表
   - 显示每个文件的相对路径和大小

4. **添加标签**
   - 在标签输入框中添加标签（必填）
   - 支持自动完成和标签提示

5. **开始上传**
   - 点击"开始上传"按钮
   - 系统会批量上传所有文件，保持文件夹结构

## 技术实现细节

### 后端实现

```python
@app.post("/upload-folder")
async def upload_folder(
    files: List[UploadFile] = File(...),
    relative_paths: List[str] = Form(...),
    tags: List[str] = Form(...),
    db: Session = Depends(get_db)
):
    # 批量处理文件上传
    # 保持文件夹结构
    # 返回上传结果
```

### 前端实现

- 使用 `webkitdirectory` 属性启用文件夹选择
- 通过 `webkitRelativePath` 获取文件的相对路径
- 批量上传时，将文件和路径信息一起发送到后端

### 文件存储结构

```
storage/
├── folder1/
│   ├── {hash}_file1.txt
│   └── subfolder/
│       └── {hash}_file2.txt
└── {hash}_single_file.txt
```

## 注意事项

1. **浏览器兼容性**: 文件夹上传功能需要浏览器支持 `webkitdirectory` 属性
   - Chrome/Edge: ✅ 支持
   - Firefox: ✅ 支持
   - Safari: ✅ 支持

2. **文件查重**: 文件夹上传时，每个文件都会进行 SHA-256 哈希值查重
   - 如果文件已存在，会跳过该文件并记录在失败列表中

3. **空文件处理**: 空文件（0字节）会被自动跳过

4. **路径处理**: 
   - 相对路径会自动规范化（去除开头的 `/` 或 `\`）
   - 文件夹结构会在 `storage/` 目录下创建

5. **标签应用**: 文件夹上传时，所有文件都会应用相同的标签

## 后续优化建议

1. 支持文件夹下载（打包成 ZIP）
2. 支持文件夹预览（树形结构展示）
3. 支持文件夹搜索（按文件夹路径搜索）
4. 支持文件夹重命名和管理
5. 支持文件夹级别的标签管理

## 测试建议

1. 测试单文件上传（确保向后兼容）
2. 测试文件夹上传（包含多层嵌套文件夹）
3. 测试重复文件处理
4. 测试空文件处理
5. 测试大文件夹上传（性能测试）
