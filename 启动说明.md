# 启动说明

## 方式一：一键启动（推荐）

### Windows 系统
双击运行 `start.bat` 文件，会自动启动后端和前端服务。

### macOS/Linux 系统
```bash
chmod +x start.sh
./start.sh
```

## 方式二：手动分别启动

### 1. 启动后端服务

在项目根目录执行：
```bash
python main.py
```

后端将在 `http://localhost:8080` 启动。

### 2. 启动前端服务

打开新的终端窗口，执行：
```bash
cd frontend
npm install    # 首次运行需要安装依赖
npm run dev
```

前端将在 `http://localhost:5173` 启动。

## 访问应用

启动成功后，打开浏览器访问：
**http://localhost:5173**

## 注意事项

1. **启动顺序**：建议先启动后端，再启动前端
2. **端口占用**：确保 8080 和 5173 端口未被占用
3. **依赖安装**：前端首次运行需要执行 `npm install`
4. **停止服务**：
   - Windows: 关闭对应的命令行窗口
   - macOS/Linux: 按 `Ctrl+C` 停止

## 开发模式

- 后端：修改代码后需要重启服务
- 前端：Vite 支持热重载，修改代码自动刷新
