# TeamHub 系统说明书

> 版本：v1.0 | 文档日期：2026-03-01

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构](#2-系统架构)
3. [安装与启动](#3-安装与启动)
4. [功能模块详解](#4-功能模块详解)
   - 4.1 [初始化向导（SetupPage）](#41-初始化向导)
   - 4.2 [欢迎页（WelcomePage）](#42-欢迎页)
   - 4.3 [文档库（LibraryPage）](#43-文档库)
   - 4.4 [待整理（PendingPage）](#44-待整理)
   - 4.5 [标签管理（TagsPage）](#45-标签管理)
   - 4.6 [回收站（TrashPage）](#46-回收站)
   - 4.7 [云仓库（SharedInboxPage）](#47-云仓库)
   - 4.8 [设置（SettingsPage）](#48-设置)
5. [全局功能](#5-全局功能)
   - 5.1 [顶部导航栏](#51-顶部导航栏)
   - 5.2 [全局拖拽上传](#52-全局拖拽上传)
   - 5.3 [桌面悬浮窗](#53-桌面悬浮窗)
   - 5.4 [查重机制](#54-查重机制)
6. [核心数据模型](#6-核心数据模型)
7. [后端 API 接口](#7-后端-api-接口)
8. [文件库目录结构](#8-文件库目录结构)
9. [设置项说明](#9-设置项说明)
10. [常见操作流程](#10-常见操作流程)

---

## 1. 系统概述

**TeamHub** 是一款**本地优先的桌面文档管理系统**，以 Electron 桌面应用的形式运行，内置 Vue 3 前端与 FastAPI/Python 后端。

### 核心定位

- 管理本地文件库中的文档，支持版本追踪与标签分类
- 通过"待整理区"作为文件进入文档库的缓冲区，提供灵活的分类整理流程
- 支持跨设备 OSS 云同步，可将文档推送至 MinIO 或阿里云 OSS 与其他设备共享

### 主要能力

| 能力 | 描述 |
|------|------|
| 文档管理 | 导入、分类、重命名、标签、搜索文档 |
| 版本控制 | 为每个文档保留历史版本，支持版本切换 |
| 标签系统 | 多标签分类，支持创建、重命名、合并、批量操作 |
| 拖拽上传 | 应用窗口直接拖入文件/文件夹，悬浮窗快速投递 |
| 查重检测 | 基于 SHA-256 哈希的精确查重，避免重复录入 |
| 智能分析 | 自动识别相似文件（潜在版本组）和标签建议 |
| 云存储同步 | 通过 OSS 跨设备共享文档（MinIO/阿里云 OSS） |
| 软删除 | 回收站机制，删除后可恢复，支持自动定期清理 |

---

## 2. 系统架构

```
┌─────────────────────────────────────────────┐
│                 Electron 外壳                 │
│  ┌─────────────┐     ┌──────────────────┐   │
│  │  主窗口      │     │  悬浮窗（80×80）  │   │
│  │  Vue 3 SPA  │     │  快速拖拽投递    │   │
│  └──────┬──────┘     └──────────────────┘   │
│         │ Axios REST (127.0.0.1:8001-8010)   │
│  ┌──────▼──────────────────────────────┐    │
│  │          FastAPI 后端               │    │
│  │  documents / versions / tags /      │    │
│  │  settings / oss / sync APIs         │    │
│  └──────┬──────────────────────────────┘    │
│         │ SQLAlchemy ORM                     │
│  ┌──────▼──────────────────────────────┐    │
│  │     SQLite 数据库（WAL 模式）         │    │
│  │  {library}/.teamhub/db.sqlite       │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
              │ boto3 S3 API
    ┌─────────▼──────────┐
    │  OSS 云存储          │
    │  MinIO / 阿里云 OSS │
    └────────────────────┘
```

**技术栈**

| 层 | 技术 |
|----|------|
| 桌面外壳 | Electron 25+ |
| 前端 | Vue 3 + Vite + Element Plus + Pinia + Vue Router |
| 后端 | FastAPI + SQLAlchemy（同步）+ SQLite（WAL 模式）|
| 云存储 | boto3（S3 兼容）+ MinIO / 阿里云 OSS |
| 打包 | PyInstaller（后端）+ electron-builder（全量）|

---

## 3. 安装与启动

### 3.1 环境依赖

```bash
# Python 3.11+
pip install -r requirements.txt

# Node.js 18+
npm install              # 根目录（Electron 依赖）
cd frontend && npm install   # 前端依赖
```

### 3.2 开发模式启动

```bash
# 方式1：完整 Electron 应用（推荐）
npm run electron:dev

# 方式2：仅后端（Web 模式，端口 8080）
python main.py

# 方式3：仅前端开发服务器（端口 5173）
cd frontend && npm run dev
```

### 3.3 生产构建

```bash
npm run build:all        # 全量构建：后端 EXE + 前端 + Electron 安装包
npm run backend:build    # PyInstaller → dist/teamhub-backend.exe
npm run frontend:build   # Vite → frontend/dist/
npm run electron:build   # electron-builder → dist-electron/
```

### 3.4 首次使用

1. 启动应用后进入**初始化向导**
2. 选择或输入一个本地目录作为文件库根目录
3. 点击"开始使用"，系统自动创建 `.teamhub/` 子目录结构
4. 进入欢迎页，可直接进入文档库

---

## 4. 功能模块详解

---

### 4.1 初始化向导

**路由**：`/setup`（首次启动或更换文件库时显示）

**功能**：选择本地目录作为文件库根目录。

**界面元素**：
- 应用名称与功能描述文字
- 文件库路径输入框
- **浏览** 按钮（桌面模式调用 Electron 原生目录选择器，Web 模式手动输入）
- 使用提示说明列表
- **开始使用** 按钮（路径为空时禁用）

**执行逻辑**：
1. 调用后端 `POST /api/settings/library` 初始化文件库
2. 自动创建以下目录：
   - `{library}/.teamhub/` — 系统配置目录
   - `{library}/.teamhub/index-versions/` — 索引模式版本存储
   - `{library}/.teamhub/trash/` — 回收站目录
   - `{library}/待整理/` — 待整理文件夹（默认名称，可修改）
3. 初始化完成后跳转到文档库

---

### 4.2 欢迎页

**路由**：应用重启后在正常布局前显示

**功能**：提供快速概览，确认用户要进入哪个文件库。

**界面元素**：
- TeamHub 图标 + 应用名称
- 当前文件库路径（带 Tooltip）
- 统计信息行：
  - 总文档数
  - 文件库总大小
  - 待整理数量（大于0时显示，橙色高亮）
- **打开文件库** 按钮
- **更换文件库** 文字链接（触发初始化向导）

---

### 4.3 文档库

**路由**：`/library`

**功能**：浏览、搜索和管理已整理的文档。

#### 4.3.1 页面布局

页面由三个区域组成：
- **左侧目录树面板**（可通过设置开关）
- **搜索与筛选区**
- **文档列表区**

#### 4.3.2 左侧目录树（LibraryTreePanel）

- 显示文件库内所有子目录的层级树结构
- 点击目录节点：将文档列表过滤为该目录下的文档
- 当前选中项：蓝色左边框（3px）+ 蓝色文字 + 半粗体
- 面板头部：浅蓝色渐变背景 + 蓝色标题文字
- 点击"文档库根目录"节点：清除目录过滤，显示全部文档

#### 4.3.3 搜索与筛选

**SmartSearchBar（智能搜索框）**：
- 实时全文搜索（匹配文档名称）
- 支持中英文关键字

**筛选栏**：
- **标签筛选**：多选下拉，已选标签以胶囊形式展示；多标签时要求 ALL 匹配
- **文件类型**：全部 / 仅文件 / 仅文件夹（三选一）
- **排序字段**：更新时间 / 名称 / 大小 / 创建时间
- **排序方向**：升序 / 降序
- **结果统计**：显示"共 X 个文档"

**扫描文件库** 按钮（始终可见）：
- 触发 `POST /api/settings/scan`
- 自动检测丢失文件，刷新列表

#### 4.3.4 文档卡片

每张卡片展示：
- **文件图标**（根据扩展名自动识别：文档/表格/图片/视频/音频/压缩包/笔记本等）
- **文档名称**（粗体，超长截断）
- **标签列表**（胶囊形 Element Plus Tag，`effect="light"`）
- **元数据行**：大小或文件数量 · 最后更新时间 · 同步状态标记
- **操作按钮区**：
  - **打开** 按钮（调用系统默认应用打开文件）
  - **更多 ▾** 下拉菜单

**下拉菜单操作**：

| 菜单项 | 说明 |
|--------|------|
| 编辑信息 | 弹出对话框，修改文档名称和描述 |
| 编辑标签 | 弹出对话框，多选添加/删除标签 |
| 版本历史 | 打开右侧文档详情 Drawer |
| 推送到云端 | 将文档上传至 OSS（选择公开/私有） |
| 关闭云同步 | 撤销 OSS 同步（仅在已启用云同步时显示） |

**悬停效果**：
- 左边框：蓝色 inset 3px
- 阴影提升（shadow-hover）
- 上移 -2px（translateY）

#### 4.3.5 文档详情 Drawer（右侧滑出）

点击"版本历史"打开，包含以下部分：

**标题区**：
- 文档名称（可点击直接内联编辑）
- 状态徽章（已整理 / 待整理 / 缺失 / 已删除）

**操作按钮行**：
- **打开文件**：用系统默认程序打开
- **定位到文件夹**：在文件资源管理器中显示
- **移回待整理**：将文档状态改回 pending（`POST /{id}/move-to-pending`）
- **删除**：软删除到回收站（二次确认）

**基本信息区**：
- 描述（可编辑，点击铅笔图标进入编辑模式）
- 当前状态 / 存储方式 / 存储路径 / 原始路径
- 创建时间 / 最后更新时间 / 文件大小

**标签区**：
- 显示所有已关联标签（胶囊形）
- 点击"编辑标签"按钮进入标签编辑

**版本历史区**：
- 列出所有版本（版本号、创建时间、文件大小、SHA256 哈希前8位）
- 当前版本标注"当前"徽章
- 每个版本可操作：
  - **设为当前版本**（`POST /api/versions/{id}/make-current`）
  - **删除此版本**（保留至少1个版本）

#### 4.3.6 缺失文件警告

当文档库中有文件已从磁盘丢失时：
- 页面顶部显示橙色警告横幅：`X 个文档文件已缺失`
- 每条记录展示：文档名、当前存储路径、两个操作按钮：
  - **重新定位**：弹出文件选择器，指向新位置（`POST /{id}/relocate`）
  - **移除**：从数据库中彻底删除该记录

#### 4.3.7 分页

- 默认每页 15 条（可在设置中修改，范围 5-100）
- Element Plus 分页组件：显示总数、上一页/下一页、页码列表

---

### 4.4 待整理

**路由**：`/pending`

**功能**：文件进入文档库前的暂存区和整理工作台。

#### 4.4.1 页面顶部

- **标题**：`待整理` + 红色角标（显示待整理数量）
- **全选 / 取消全选** 按钮（有文档时显示）
- **智能扫描** 按钮（`GET /api/documents/analyze-pending`）

#### 4.4.2 智能扫描结果面板

点击"智能扫描"后，在文档列表上方展开分析结果面板（可关闭）：

**版本组（相似文件）**：
- 系统自动识别文件名相似的文档（如 `报告_v1.docx`、`报告_v2.docx`）
- 每组展示：基础文件名、文件数量、各文件列表（名称 + 大小）
- **合并为版本** 按钮（复用合并对话框，自动填充建议名称）

**标签建议**：
- 基于文件名模式推断可能适合的标签
- 每条展示：建议标签（胶囊）、推荐理由文字
- **应用** 按钮：为关联的所有文档批量打上该标签

**空结果**：若无相似文件或建议，显示"未发现相似文件或可建议的标签"

#### 4.4.3 缺失文件警告

- 橙色 alert 横幅，列出已从磁盘丢失的待整理文件
- 每条：警告图标 / 文档名 / 存储路径 / **移除** 按钮（仅删除数据库记录）
- **重新扫描** 按钮

#### 4.4.4 待整理文档卡片

每张卡片展示：
- 复选框（批量选择）
- 文件图标（自动识别类型）
- 文档名称
- 大小（单文件）或文件数量（文件夹） · 创建时间
- **操作按钮区**：
  - **打开**：用系统默认程序打开
  - **撤销**（仅 `storage_mode=move` 时显示）：将文件移回原始位置并删除记录
  - **整理**：打开整理对话框

**悬停效果**：橙色（`#e6a23c`）inset 3px 左边框 + 阴影提升

**点击卡片（非按钮区域）**：打开文档详情 Drawer

#### 4.4.5 整理对话框

点击**整理**按钮打开，包含：

| 字段 | 说明 |
|------|------|
| 文档名称 | 可修改（文件扩展名自动保留，文件夹无扩展名） |
| 存放到（必填） | 目录树选择器：显示文件库所有子目录，可选择根目录 |
| 标签（必填） | 多选 + 输入创建，至少需要1个；Enter 键快速添加 |

- 点击**完成整理**：调用 `POST /{id}/organize`，文档移入文档库
- 整理成功后关闭对话框，刷新待整理列表和计数角标

#### 4.4.6 撤销操作

- 点击卡片上的**撤销**按钮：调用 `POST /{id}/undo-pending`
- 后端将文件从待整理目录移回 `original_path`（原始位置）
- 从数据库中删除该文档记录
- 若原始位置已有同名文件，撤销失败并提示错误

#### 4.4.7 批量操作栏

选中1个或多个文档后，页面底部固定显示批量操作栏（深色底条）：

| 按钮 | 触发条件 | 功能 |
|------|----------|------|
| 批量整理 | 1+ 选中 | 打开批量整理对话框（共享目标目录 + 标签） |
| 批量打标签 | 1+ 选中 | 打开标签批量添加/删除对话框 |
| 合并为版本 | 2+ 选中 | 打开合并对话框，将所选文档合并为同一文档的多个版本 |
| 删除 | 1+ 选中 | 永久删除所选文档（二次确认，不进回收站） |

**批量整理对话框**：
- 存放到：目录树选择器（与单文档整理相同）
- 标签：多选 + 创建，至少1个（所有选中文档共用同一套标签）

**批量打标签对话框**：
- 添加标签：多选（从已有标签中选或新建）
- 移除标签：多选（从已有标签中选）
- 两者可同时操作

**合并为版本对话框**：
- 预览所选文档列表（名称 + 大小 + 创建时间）
- 合并后名称（默认取第一个文档的文件名去扩展名）
- 描述（可选）
- 确认后：最新的文件成为当前版本，其余作为历史版本

#### 4.4.8 分页

同文档库，支持分页浏览。

---

### 4.5 标签管理

**路由**：`/tags`

**功能**：集中管理所有标签，查看使用统计，执行合并、重命名、删除操作。

#### 4.5.1 顶部统计卡片

三个数值卡片（横向排列）：
- **标签总数**：系统中所有标签数量
- **关联文件总数**：所有标签关联的文档总数（去重后）
- **平均关联数**：每个标签平均关联的文档数

#### 4.5.2 操作按钮栏

| 按钮 | 说明 |
|------|------|
| 创建标签 | 弹出对话框，输入标签名称（最多50字符，显示字数限制） |
| 合并标签 | 选中2+个标签时激活；弹出合并对话框 |
| 批量删除 | 选中1+个标签时激活；二次确认后批量删除 |
| 刷新 | 重新加载标签列表 |

#### 4.5.3 标签列表表格

| 列 | 说明 |
|----|------|
| 复选框 | 批量选择 |
| 标签名称 | 支持双击或点击"编辑"按钮内联修改 |
| 关联文件数 | 颜色编码：灰色=0，橙色<5，蓝色<20，绿色≥20 |
| 操作 | 查看文件 / 编辑 / 删除 |

**查看文件**：跳转到文档库页面并自动应用该标签过滤器

**编辑**：
- 点击后标签名称变为输入框
- 输入新名称后按 Enter 或点击确认保存
- 调用 `PUT /api/tags/{id}`

**删除**：
- 二次确认
- 从所有文档中移除该标签，并删除标签本身
- 调用 `DELETE /api/tags/{id}`

#### 4.5.4 合并标签对话框

1. 展示所选标签列表（名称 + 关联文件数）
2. 选择目标标签（从所选标签中选一个作为保留项）
3. 确认后：
   - 所有关联到源标签的文档改为关联目标标签
   - 源标签从系统中删除
   - 调用 `POST /api/tags/merge`

---

### 4.6 回收站

**路由**：`/trash`

**功能**：查看、恢复或永久删除已软删除的文档。

#### 4.6.1 页面顶部

- **标题**：`回收站` + 数量角标
- **刷新** 按钮
- **清空回收站** 按钮（危险操作，二次确认）

#### 4.6.2 回收站文档卡片

每张卡片展示：
- 复选框
- 文件图标（类型识别）
- 文档名称
- 原始存储路径（提示文件曾经在哪里）
- 元数据：大小 / 文件数 · 删除时间
- **操作按钮**：
  - **恢复**：从回收站恢复为已整理状态（`POST /api/documents/batch/restore`）
  - **永久删除**：二次确认后彻底删除（磁盘文件同步删除，`DELETE /{id}?permanent=true`）

**悬停效果**：红色（`#e74c3c`）inset 3px 左边框 + 阴影提升

#### 4.6.3 批量操作栏

选中文档后底部固定显示：
- **批量恢复**：将所选文档全部恢复
- **批量永久删除**：二次确认后彻底删除所有选中项

#### 4.6.4 清空回收站

- 点击页面顶部"清空回收站"按钮
- 弹出确认框显示当前回收站文档数量
- 确认后调用 `POST /api/settings/trash/clean`
- 物理删除所有回收站文件和数据库记录

---

### 4.7 云仓库

**路由**：`/shared`（SharedInboxPage）

**功能**：通过 OSS 查看其他设备推送的文档，进行跨设备文档交换。

#### 4.7.1 未配置 OSS 时

- 显示引导状态（空状态插图 + 文字说明）
- 提示用户前往设置页面配置 OSS

#### 4.7.2 已配置 OSS 时

**搜索面板**（与文档库一致）：
- SmartSearchBar（文件名关键字搜索，客户端过滤）
- 排序字段 + 排序方向
- 结果统计

**文档卡片（来自 OSS Inbox）**：
- 蓝色文件图标
- 文档名称
- 标签列表（胶囊形）
- 设备信息：设备名称 + `本设备` 标签（本机推送的文档）
- `私有` 标签（若 `share_visibility=private`）
- 大小 + 推送时间戳

**操作按钮（根据来源设备判断）**：

| 场景 | 可用操作 |
|------|----------|
| 其他设备的文档 | **导入**（到待整理区）、**下载**（保存到本地指定目录） |
| 本设备推送的文档 | **撤回**（从 OSS 删除）|

**导入操作**：
- 调用 `POST /api/oss/import`
- 从 OSS 下载文件到 `.teamhub/inbox/`，并创建待整理文档记录

**下载操作**：
- 调用 Electron `selectDirectory()` 选择保存目录
- 调用 `POST /api/oss/download` 将文件下载到用户指定位置

**撤回操作**：
- 二次确认后调用 `POST /api/oss/delete`
- 从 OSS 删除该文档的所有版本文件和元数据

---

### 4.8 设置

**路由**：`/settings`

**功能**：配置应用行为、管理文件库、云存储配置。

页面包含 **4 张设置卡片**，以下分别说明：

---

#### 卡片1：文件库总览

**只读展示区**：
- 文件库路径（完整绝对路径）
- 待整理路径（`{library}/{待整理文件夹名}`）+ **修改** 按钮

**统计数据（4格）**：
- 总文档数
- 文件库大小
- 待整理数量
- 回收站数量 + 占用空间

**修改待整理文件夹对话框**：
- 输入新文件夹名称（不能为空，不能含路径分隔符）
- 处理方式（单选）：
  - **迁移文件**：将当前待整理目录内所有文件物理移动到新目录，并更新数据库中的 `storage_path`
  - **仅清空记录**：仅删除数据库中所有待整理文档记录，不移动、不删除磁盘文件
- 选择"仅清空记录"时显示橙色警告提示
- 确认后调用 `PUT /api/settings/pending-path`

---

#### 卡片2：使用偏好

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 默认存储方式 | 单选 | **移动**（从原位置移入库）/ **复制**（原位置保留）/ **索引**（原位置不动，仅建立索引） |
| 单文件大小上限 | 数字输入 | 单位 MB，超出此限制的文件将被拒绝导入 |
| 同名文件冲突处理 | 单选 | **自动重命名**（追加序号）/ **追加为新版本** |
| 文档库默认排序 | 下拉+单选 | 排序字段（更新时间/名称/大小/创建时间）+ 方向（升序/降序） |
| 每页显示数量 | 数字输入 | 范围 5-100，步长 5 |
| 平铺展示 | 开关 | 是否显示文档库平铺列表（关闭后隐藏主列表，仅保留目录树） |
| 目录树展示 | 开关 | 是否显示左侧目录树面板 |
| 桌面悬浮窗 | 开关 | 是否启用始终置顶的小型拖拽投递窗口 |

点击 **保存设置** 调用 `PUT /api/settings/config`。

---

#### 卡片3：维护与清理

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 启动时自动扫描 | 开关 | 应用启动时自动检查文件库中是否有文件丢失 |
| 最大版本数 | 数字输入 | 每个文档最多保留的版本数量（0 = 不限制） |
| 版本保留天数 | 数字输入 | 超过此天数的旧版本自动清理（0 = 永久保留） |
| 回收站自动清理 | 数字输入 | 超过此天数的回收站文档自动永久删除（0 = 不自动清理） |

点击 **保存设置** 调用 `PUT /api/settings/config`。

**维护操作（横排三个按钮）**：

| 按钮 | API | 说明 |
|------|-----|------|
| 立即清空回收站 | `POST /api/settings/trash/clean` | 弹出确认框（显示数量），执行后永久删除所有回收站文档 |
| 立即版本清理 | `POST /api/settings/versions/clean` | 按当前"最大版本数"和"版本保留天数"设置，删除超额的旧版本 |
| 扫描文件库 | `POST /api/settings/scan` | 检测所有已整理文档的磁盘文件是否存在，标记缺失文件 |

---

#### 卡片4：云存储（OSS/MinIO）

**状态标签**：已配置（绿色）/ 未配置（灰色）

**已配置时显示统计（4格）**：
- 已启用同步（`cloud_sync_enabled=true` 的文档数）
- 待推送（有变更尚未同步的文档数）
- 冲突文档（sync_status=conflict 的文档数）
- 云仓库（OSS 中存储的文档总数）

**配置表单**：

| 字段 | 说明 |
|------|------|
| Endpoint 地址 | OSS 服务地址（如 `http://localhost:9000` 或 `https://oss-cn-hangzhou.aliyuncs.com`）|
| Access Key ID | OSS 访问密钥 ID |
| Access Key Secret | OSS 访问密钥（密码字段，可显示/隐藏） |
| Bucket 名称 | 存储桶名称 |
| 存储路径前缀 | 文件在 Bucket 内的路径前缀（如 `teamhub/`） |

- **保存配置** 按钮：调用 `PUT /api/oss/config`
- **测试连接** 按钮：调用 `GET /api/oss/test-connection`，显示成功/失败 alert

**说明提示**：MinIO 本地部署注意事项（默认端口、凭据等）

---

## 5. 全局功能

---

### 5.1 顶部导航栏

导航栏固定在页面顶部（`position: sticky; z-index: 1000`），渐变深色背景。

**Logo 区（左）**：
- TeamHub 图标 + 应用名称，点击跳转到 `/library`

**主导航（中）**：

| 导航项 | 路由 | 图标 | 特殊说明 |
|--------|------|------|----------|
| 文档库 | `/library` | Folder | — |
| 待整理 | `/pending` | Download | 有待处理文件时显示红色数字角标 |
| 标签管理 | `/tags` | PriceTag | — |
| 云仓库 | `/shared` | Share | — |

**激活状态**：蓝色底部边框（2px）+ 浅蓝色背景

**次级导航（右）**：
- **回收站** 链接
- **⚙ 设置** 齿轮图标按钮

---

### 5.2 全局拖拽上传

将文件或文件夹直接拖入应用窗口，即可快速添加到待整理区。

#### 拖拽覆盖层

- 拖入文件时显示蓝色半透明遮罩 + 提示文字"松开以添加到待整理"
- 松开后进入处理流程

#### 单文件处理流程

1. **查重检测**（`POST /api/documents/check-duplicate`）：
   - 已在待整理中：弹出确认框，可选"仍然添加"或"前往查看"
   - 已在文档库中：弹出确认框，可选"仍然添加"或"前往查看"
   - 无重复：直接添加
2. **调用** `POST /api/documents/quick-add`（source_path, is_folder=false）
3. **显示通知**（右下角，持续8秒）：
   - 文件名
   - **查看** 按钮（跳转到待整理页面）
   - **撤销** 按钮（调用 `POST /{id}/undo-pending`，将文件移回原位）
4. 待整理计数角标自动刷新

#### 文件夹处理流程

弹出对话框询问整理方式：

**打散（Scatter）**：
1. 递归遍历文件夹所有文件（包含子文件夹）
2. 遇到子文件夹，再次询问是否打散该子文件夹
3. **预扫描阶段**：对所有文件批量查重（显示 Loading 遮罩）
4. 发现重复文件时：弹出汇总对话框（展示所有重复文件的表格）
   - **全部添加（含重复）**
   - **跳过重复，添加其余**
   - **取消全部**
5. 显示进度对话框（含百分比进度条、当前处理文件名、已处理/成功/失败/跳过计数）
6. 可随时点击**取消**中断
7. 结束后显示摘要通知（含批量撤销按钮）

**不打散（Keep as Folder）**：
1. 对整个文件夹批量查重（`POST /api/documents/check-folder-duplicates`）
2. 发现重复时展示汇总对话框（仅"仍然添加"或"取消"）
3. 确认后以文件夹为整体调用 `POST /api/documents/quick-add`（is_folder=true）

---

### 5.3 桌面悬浮窗

仅在 Electron 桌面模式下可用。

**外观**：
- 80×80 像素半透明圆角小窗口
- 始终置顶（`always-on-top`）
- 可拖动任意位置

**功能**：
- 将文件/文件夹拖拽到悬浮窗即可触发添加到待整理
- 触发后主窗口自动前置（确保用户能看到通知/对话框）
- 与主窗口拖拽完全相同的处理逻辑（查重 → 确认 → 添加 → 通知）

**控制**：
- 在设置页"桌面悬浮窗"开关控制显示/隐藏
- 切换后立即生效（IPC 调用 `toggleFloatingWindow(enabled)`）

---

### 5.4 查重机制

TeamHub 使用 **SHA-256 哈希精确查重**。

**查重时机**：
- 文件拖入主窗口或悬浮窗时（每个文件独立查重）
- 文件夹打散模式：预扫描阶段批量查重

**查重范围**：
- 先与**已整理文档库**（status=organized）的 Version 记录比对
- 再与**待整理区**（status=pending）的 Version 记录比对

**哈希计算时机**：
- 快速添加（quick-add）时：同步计算 SHA-256（确保立即可查重）
- 若同步计算失败：后台任务补算
- 完整导入流程：同步计算

---

## 6. 核心数据模型

### Document（文档）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID v7 主键 |
| name | String(255) | 文档名称（用户可修改）|
| description | Text | 备注说明（可选）|
| is_folder | Boolean | 是否为文件夹 |
| storage_path | String | 相对于文件库根目录的路径（索引模式为 null）|
| storage_mode | String | `move`（移动）/ `copy`（复制）/ `index`（索引）|
| original_path | String | 文件导入前的原始绝对路径 |
| status | String | `pending`（待整理）/ `organized`（已整理）/ `missing`（缺失）/ `trashed`（回收站）|
| file_count | Integer | 文件数量（文件夹文档为内含文件总数）|
| total_size | BigInteger | 文件总大小（字节）|
| created_at | DateTime | 创建时间（UTC）|
| updated_at | DateTime | 最后更新时间（UTC）|
| trashed_at | DateTime | 删除时间（UTC，软删除）|
| sync_status | String | `local`（本地）/ `synced`（已同步）/ `conflict`（冲突）|
| cloud_sync_enabled | Boolean | 是否启用云同步 |
| share_visibility | String | `public`（公开）/ `private`（私有）|
| tags | M:N | 关联标签（通过 document_tag 表）|
| versions | 1:N | 版本历史 |

### Version（版本）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID v7 主键 |
| document_id | String(36) | 所属文档 ID |
| version_number | Integer | 版本号（从1递增）|
| file_path | String | 版本文件的绝对路径 |
| relative_path | String | 文件夹文档内的相对路径（null 表示主文件）|
| sha256_hash | String(64) | 文件 SHA-256 哈希值（查重依据）|
| file_size | BigInteger | 版本文件大小（字节）|
| original_filename | String | 导入前的原始文件名 |
| note | Text | 版本备注（可选）|
| is_current | Boolean | 是否为当前版本 |
| created_at | DateTime | 创建时间（UTC）|
| sync_status | String | `local` / `synced` |

### Tag（标签）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID v7 主键 |
| name | String(100) | 标签名称（唯一）|
| color | String(7) | 颜色十六进制值（如 `#4096ff`，可选）|
| documents | M:N | 关联文档 |

### ChangeLog（变更日志）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String(36) | UUID v7 主键 |
| device_id | String(36) | 发生变更的设备 ID |
| entity_type | String(20) | `document` / `version` / `tag` |
| entity_id | String(36) | 对应实体的 UUID（NOT NULL）|
| operation | String(20) | `create` / `update` / `delete` / `trash` / `restore` / `tag_add` / `tag_remove` |
| payload | Text | JSON 快照（变更后的关键字段）|
| changed_at | DateTime | 变更时间（UTC）|

---

## 7. 后端 API 接口

所有接口以 `/api/` 为前缀，健康检查：`GET /api/health`。

### 7.1 文档接口 `/api/documents`

| Method | 路径 | 说明 |
|--------|------|------|
| POST | `/` | 完整导入文档（指定目标目录和标签）|
| GET | `/{id}` | 获取文档详情 |
| PUT | `/{id}` | 修改文档名称/描述 |
| DELETE | `/{id}` | 软删除（移入回收站），加 `?permanent=true` 永久删除 |
| POST | `/search` | 搜索文档（支持关键字/标签/状态/排序/分页）|
| PUT | `/{id}/tags` | 更新文档标签列表 |
| POST | `/{id}/organize` | 整理文档（待整理 → 文档库，指定目录和标签）|
| POST | `/{id}/relocate` | 重新定位丢失文件 |
| POST | `/{id}/move-to-pending` | 将已整理文档移回待整理区 |
| POST | `/{id}/undo-pending` | 撤销待整理：文件移回原位并删除记录 |
| POST | `/quick-add` | 快速添加到待整理区（拖拽投递用）|
| GET | `/pending/count` | 获取当前待整理文档数量 |
| POST | `/analyze-pending` | 智能分析待整理区（版本组+标签建议）|
| POST | `/check-duplicate` | 单文件查重（SHA-256）|
| POST | `/check-folder-duplicates` | 文件夹批量查重 |
| POST | `/batch/organize` | 批量整理 |
| POST | `/batch/tags` | 批量添加/删除标签 |
| POST | `/batch/delete` | 批量永久删除 |
| POST | `/batch/restore` | 批量从回收站恢复 |
| POST | `/merge-versions` | 合并多个文档为同一文档的版本 |

### 7.2 版本接口 `/api/versions`

| Method | 路径 | 说明 |
|--------|------|------|
| GET | `/{doc_id}` | 获取文档的所有版本列表 |
| POST | `/` | 为文档新增版本 |
| DELETE | `/{id}` | 删除指定版本（至少保留1个）|
| POST | `/{id}/make-current` | 将指定版本设为当前版本 |
| POST | `/cleanup` | 按保留策略清理超额旧版本 |

### 7.3 标签接口 `/api/tags`

| Method | 路径 | 说明 |
|--------|------|------|
| GET | `/` | 获取所有标签（含关联文档数）|
| POST | `/` | 创建新标签 |
| PUT | `/{id}` | 重命名标签 |
| DELETE | `/{id}` | 删除标签（从所有文档中移除）|
| POST | `/merge` | 合并标签 |

### 7.4 设置接口 `/api/settings`

| Method | 路径 | 说明 |
|--------|------|------|
| GET | `/library` | 获取文件库信息（路径/状态/统计）|
| POST | `/library` | 初始化或切换文件库 |
| GET | `/config` | 获取所有配置项 |
| PUT | `/config` | 更新配置项 |
| PUT | `/pending-path` | 修改待整理文件夹名称（含迁移或清空选项）|
| POST | `/scan` | 扫描文件库（检测丢失文件）|
| POST | `/trash/clean` | 清空回收站 |
| POST | `/versions/clean` | 清理超额版本 |
| GET | `/library/subdirs` | 列出文件库子目录（一层）|
| GET | `/library/dirtree` | 获取文件库目录树（递归）|
| POST | `/library/mkdir` | 在文件库内新建目录 |

### 7.5 OSS 云存储接口 `/api/oss`

| Method | 路径 | 说明 |
|--------|------|------|
| GET | `/config` | 获取 OSS 配置（密钥已脱敏）|
| PUT | `/config` | 保存 OSS 配置 |
| GET | `/test-connection` | 测试 OSS 连接 |
| GET | `/status` | 获取同步状态统计 |
| GET | `/inbox` | 获取其他设备推送的文档列表 |
| POST | `/push` | 推送文档到 OSS |
| POST | `/import` | 从 OSS 导入文档到待整理区 |
| POST | `/download` | 从 OSS 下载文档到指定目录 |
| POST | `/delete` | 从 OSS 撤回/删除文档 |

---

## 8. 文件库目录结构

```
{library_path}/                         # 用户选择的文件库根目录
├── 待整理/                             # 待整理区（文件夹名可修改）
│   ├── 文件A.pdf
│   └── 文件B.docx
│
├── 项目文档/                           # 整理后的子目录（用户自定义）
│   ├── 合同/
│   └── 方案/
│
└── .teamhub/                           # 系统隐藏目录（勿手动修改）
    ├── db.sqlite                       # SQLite 数据库（WAL 模式）
    ├── db.sqlite-shm                   # WAL 共享内存文件
    ├── db.sqlite-wal                   # WAL 日志文件
    ├── config.json                     # 文件库级配置（含设备ID）
    ├── index-versions/                 # 索引模式的版本文件存储
    │   └── {doc_id}/
    │       └── v1_{filename}
    └── trash/                          # 回收站（软删除的文件）
        └── {trashed_filename}
```

**config.json 典型内容**：
```json
{
  "library_path": "C:/Users/User/Documents/MyLibrary",
  "device_id": "a1b2c3d4-...",
  "device_name": "DESKTOP-XXXX",
  "pending_folder_name": "待整理",
  "default_storage_mode": "move",
  "max_versions": 0,
  "trash_auto_clean_days": 30
}
```

---

## 9. 设置项说明

| 设置项 | 默认值 | 说明 |
|--------|--------|------|
| `default_storage_mode` | `move` | 文件导入方式：移动/复制/索引 |
| `max_versions` | `0` | 最大版本数（0=不限）|
| `max_version_age_days` | `0` | 版本保留天数（0=永久）|
| `trash_auto_clean_days` | `30` | 回收站自动清理天数（0=不清理）|
| `max_file_size` | `1073741824` | 单文件大小上限（字节，默认1GB）|
| `on_conflict` | `rename` | 同名冲突处理：`rename`（重命名）/ `new_version`（新版本）|
| `default_sort_by` | `updated_at` | 默认排序字段 |
| `default_sort_order` | `desc` | 默认排序方向 |
| `auto_scan_on_startup` | `false` | 启动时自动扫描 |
| `enable_floating_window` | `true` | 启用悬浮窗 |
| `items_per_page` | `15` | 每页显示条数 |
| `library_show_flat_view` | `true` | 显示平铺列表 |
| `library_show_tree_view` | `true` | 显示目录树 |
| `pending_folder_name` | `待整理` | 待整理文件夹名称 |

---

## 10. 常见操作流程

### 10.1 第一次使用

```
启动应用 → 初始化向导 → 选择文件库目录 → 开始使用
                                    ↓
                          自动创建 .teamhub/ 目录结构
                          自动创建"待整理"文件夹
```

### 10.2 拖入文件并整理

```
拖入文件到窗口/悬浮窗
    ↓
查重检测（哈希比对）
    ├── 无重复 → 直接进入待整理
    └── 有重复 → 弹窗提示 → 用户选择"仍然添加"或"前往查看"
    ↓
待整理区出现新卡片（角标+1）
    ↓
点击"整理" → 填写存放位置 + 至少1个标签 → 完成整理
    ↓
文档库中可见（status=organized）
```

### 10.3 版本管理

```
已整理文档 → 点击"版本历史"(Drawer) → 查看所有版本
    ↓
新增版本：拖入同文档的新版本文件 → 整理时选择"追加为新版本"
    ↓
切换版本：在 Drawer 中点击某版本的"设为当前版本"
    ↓
删除旧版本：在 Drawer 中点击某版本的"删除"（至少保留1个）
```

### 10.4 批量整理

```
待整理页 → 勾选多个文档 → 底部出现批量操作栏
    ↓
批量整理 → 选择共同存放位置 + 共同标签 → 完成
    ↓
所有选中文档同时整理到同一目录，打上相同标签
```

### 10.5 文件丢失处理

```
文件库页面出现橙色警告横幅（缺失 X 个文档）
    ↓
点击"重新定位" → 选择文件在磁盘上的新位置 → 更新存储路径
    ↓
或点击"移除" → 从数据库删除该记录（不影响磁盘文件）
    ↓
或点击"扫描文件库" → 系统自动检测已移回原位的文件
```

### 10.6 跨设备文档共享

```
设备A：设置 → 配置 OSS → 保存
    ↓
设备A：文档库 → 文档菜单 → 推送到云端 → 选择公开/私有
    ↓
（文件上传至 OSS Bucket）
    ↓
设备B：配置相同 OSS → 云仓库 → 看到设备A推送的文档
    ↓
设备B：点击"导入" → 文档进入设备B的待整理区
    ↓
设备B：整理后进入本地文档库
```

---

*本说明书由系统自动生成，描述 TeamHub v1.0 全部功能。如有功能更新，请同步更新本文档。*
