teamhub本地文档整理软件


1.将对象拖拽到悬浮窗或electron窗口
2.判断对象类型
    2.1 如果是文件，则直接将对象移入待整理区域
    2.2 如果是文件夹，则询问用户文件夹的整理方式
        2.2.1 用户选择打散，则递归文件夹第一层文件，如果第一层内存在文件夹，继续询问用户是否需要打散，整理原则，遇到文件夹时都需要询问用户是否需要打散处理
        2.2.2 用户选择不打散，则将整个文件夹作为一个整体对象，将文件夹整体移入待整理区域
        2.2.3 用户选择取消，则取消操作
    2.3 对象移动后，建立对象的元数据文件，对象元数据文件需包含文件HASH值、对象名、对象大小、对象创建时间、对象修改时间、对象类型
    2.4 对象移动后，建立对象的索引文件，对象索引文件包含对象HASH值、对象原路径、对象当前路劲
3. 文件进入待整理区的操作
    3.1 打开操作：点击【打开】按钮，打开对象
    3.2 整理操作：点击【整理】按钮
        3.2.1 可以修改对象名称
        3.2.2 可以选择存放路径，如果路径下面没有文件夹，可以直接创建一个文件夹，将对象移动到该文件夹下
        3.2.3 给对象打标签
            3.2.3.1 打标签是必选项
            3.2.3.2 每个文件至少有一个标签，最多不超过6个标签，标签最大数量在设置页面能设置
            3.2.3.3 输入标签关键字时，如果系统标签库没有该标签，则创建该标签，新标签自动进入标签库
                3.2.3.3.1 新建的标签，添加到元数据文件，同时将标签同步存放到索引文件，便于后续采用标签搜索时实现多标签联合渐进式搜索
            3.2.3.4 输入标签关键字时，如果系统标签库有该标签，则下拉显示该标签，回车确认选择该标签
            3.2.3.5 创建标签的管理页面，对所有标签进行管理
        3.2.4 将被整理对象的HASH值与文档库对象的HASH比对，如果文档库对象存在相同HASH值，则提示用户文档库已经存在相同文件，并向用户显示跳转按钮
    3.3 撤销正路操作：点击【撤销整理】按钮，则该对象读取元数据文件的原路径，对象退回到拖拽前的路径
    3.4 删除操作：点击【删除】按钮，则该对象删除
    3.5 定位到文件夹操作：点击【定位到文件夹】按钮，则打开文件夹所在目录
    3.6 文档详情：点击【详情】按钮，则读取元数据文件，显示信息，包含文件大小、文件路径
4.文件在文档库的操作
    4.1 打开操作：点击【打开】按钮，则打开文件
    5.2 编辑标签操作：点击【编辑标签】按钮，可以对文件进行打标签处理，标签处理逻辑类似3.2.3
    5.3 推送到云端操作：点击【推送到云端】按钮
        5.3.1 向云端服务器发出请求，云端服务器验证登录状态，如果登录状态正常，则将对象实体推送到云端服务器，云端服务器保存对象实体及其元数据文件、索引文件
    5.4 退回待整理操作：点击【退回待整理】按钮，将文件退回到待整理区域
    5.5 文档库的比对操作：点击点击【比对文档】，将云仓库文件与本地文档库文件进行比对，如两边文件不一致，则在对象卡片上标注有更新，文档比对的范围，仅限于本地推送到云端的对象和从云端同步到本地文档库的对象，其余的文件不进行比对
6.文档库的搜索功能
    6.1 文档库搜索共两种搜索方式
        6.1.1 方式一：通过文件名搜索
        6.1.2 方式二：通过标签搜索
            6.1.2.1 通过标签搜索时，在输入框输入关键字，如果标签库标签包含关键字，则下拉显示含有关键字的标签，选中标签后，则将包含该标签的文件加入搜索结果中，下拉显示
            6.1.2.2 渐进式搜索，每选中一个标签，则将包含该标签的文件加入搜索结果中，下拉显示，输入第二标签后，在前一搜索结果中筛选出包含第二个标签的文件，，以此类推，最终找到用户需要的文件
7、云仓库
云仓库用来存储用户【推送到云端】的文件及其元数据文件、索引文件等
    7.1 登录状态验证
        7.1.1 如用户已经登录云仓库，则显示云仓库文件列表及搜索框
        7.1.2 如用户未登录云仓库，则显示登录按钮
    7.2 云仓库的操作
        7.2.1 同步到本地文档库操作：点击【同步到文档库】，将云仓库文件同步到本地文档库
        7.2.2 文档比对操作：点击【比对文档】，将云仓库文件与本地文档库文件进行比对，如两边文件不一致，则在对象卡片上标注有更新，文档比对的范围，仅限于本地推送到云端的对象和从云端同步到本地文档库的对象，其余的文件不进行比对
    7.3 云仓库搜索功能
        7.3.1 搜索功能与6.文档库的搜索功能一样

