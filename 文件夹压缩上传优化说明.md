# 文件夹压缩上传优化说明

## 优化内容

已优化文件夹上传流程，现在采用**先压缩后上传**的方式：

1. **前端压缩**：使用 JSZip 在客户端将文件夹压缩为 ZIP 文件
2. **查重优化**：对压缩后的 ZIP 文件进行 SHA-256 哈希值查重
3. **标签处理**：压缩包作为一个整体进行标签管理

## 优势

1. **保持完整性**：整个文件夹作为一个 ZIP 文件存储，保证项目结构完整
2. **查重准确**：基于整个文件夹内容的哈希值查重，更准确
3. **减少记录**：一个文件夹对应一条数据库记录，而不是多个文件多条记录
4. **传输效率**：压缩后文件更小，上传更快

## 实现细节

### 前端实现

1. **压缩阶段**：
   - 使用 JSZip 库压缩文件夹
   - 显示压缩进度（0-95%）
   - 保持文件夹内部结构

2. **上传阶段**：
   - 将压缩后的 ZIP 文件上传
   - 显示上传进度（0-90%）
   - 校验阶段（90-100%）

### 后端实现

1. **接收 ZIP 文件**：通过 `/upload` 接口接收
2. **标识处理**：通过 `is_folder_archive` 参数标识为文件夹压缩包
3. **存储方式**：ZIP 文件作为单个文件存储
4. **元数据存储**：文件夹名称存储在 `relative_path` 字段中

## 使用步骤

1. **安装前端依赖**：
   ```bash
   cd frontend
   npm install
   ```
   这会自动安装 `jszip` 库

2. **选择文件夹上传模式**

3. **选择文件夹**：点击或拖拽文件夹

4. **预览文件夹内容**：查看文件夹中的所有文件

5. **添加标签**：为文件夹添加标签

6. **开始上传**：
   - 系统自动压缩文件夹
   - 压缩完成后自动上传
   - 进行查重和标签处理

## 技术要点

### JSZip 压缩配置

- **压缩级别**：6（平衡压缩率和速度）
- **压缩算法**：DEFLATE
- **并发处理**：每批处理 10 个文件，避免内存占用过大

### 进度显示

- **压缩进度**：0-95%（基于文件数量）
- **上传进度**：0-90%（基于文件大小）
- **校验进度**：90-100%（SHA-256 计算）

### 错误处理

- 压缩失败：显示错误信息
- 上传失败：显示错误信息
- 重复文件：显示已存在的文件信息

## 注意事项

1. **浏览器兼容性**：需要支持 File API 和 Web Workers（现代浏览器都支持）

2. **内存使用**：大文件夹压缩可能占用较多内存，建议：
   - 单个文件夹不超过 500MB
   - 文件数量不超过 1000 个

3. **压缩时间**：压缩时间取决于文件数量和大小，大文件夹可能需要较长时间

4. **文件格式**：压缩后的 ZIP 文件使用标准 ZIP 格式，可以在任何系统上解压

## 后续优化建议

1. 支持断点续传（大文件）
2. 支持压缩级别选择
3. 支持文件夹预览（解压预览）
4. 支持文件夹下载（解压下载）
5. 支持文件夹版本管理
