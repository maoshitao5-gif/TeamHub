# 数据库迁移说明

## 问题
添加了 `relative_path` 字段到 `File` 模型后，数据库表结构需要更新。

## 解决方案

### 方法一：运行迁移脚本（推荐）
```bash
python migrate_add_relative_path.py
```

### 方法二：手动执行 SQL（如果迁移脚本失败）
```sql
ALTER TABLE files ADD COLUMN relative_path VARCHAR(500) NULL;
```

### 方法三：删除数据库重新创建（会丢失所有数据）
```bash
# Windows
del teamhub.db

# Linux/Mac
rm teamhub.db
```
然后重启后端服务，SQLAlchemy 会自动创建新表。

## 重要提示

**迁移完成后，必须重启后端服务**，让 SQLAlchemy 重新加载模型定义。

如果仍然报错，请检查：
1. 数据库文件是否被其他进程锁定
2. SQLAlchemy 是否使用了正确的数据库文件路径
3. 后端服务是否完全重启
